- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # ~/.vim: vim
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/repos

- shell:
    # - [git submodule update --init --recursive, Installing submodules]
