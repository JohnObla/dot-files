- defaults:
    link:
      relink: true
    shell:
      command:
        stdout: true
        stderr: true

- clean: ['~']

- link:
    ~/.config:
    ~/.gitconfig:

- create:
    - ~/repos

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    -
      command: './.custom-scripts/update-packages.sh'
    - 
      command: './.custom-scripts/oh-my-fish-install.sh'

    - [chsh -s /usr/bin/fish, Make fish the default shell] 
      
