- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.config:

    # ~/.config/fish/conf.d/omf.fish:
    #   path: config/fish/conf.d/omf.fish
    #   create: true

    # ~/.config/omf/init.fish:
    #   path: config/omf/init.fish
    #   create: true

    # ~/.config/omf/aliases.fish:
    #   path: config/omf/aliases.fish 
    #   create: true

- create:
    - ~/repos

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    -
      command: './.custom-scripts/update-packages.sh'
      stdout: true
    - 
      command: './.custom-scripts/oh-my-fish-install.sh'
      stdout: true
      stderr: true stderr: true 
    # -
    #   command: './.custom-scripts/homebrew-install.sh'
    #   stdout: true
    #   stderr: true